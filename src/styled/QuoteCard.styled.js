import styled from 'styled-components';

const StyledQuoteCard = styled.div`
  width: 70vh;
  background-color: #14213d;
  color: #fca311;
  border-radius: 20px;
  display: flex;
  flex-direction: column;
  justify-content: space-around;
  align-items: center;
  margin: 0 auto;
  margin-top: 12rem;

  -webkit-box-shadow: 8px 8px 24px 0px rgba(20, 33, 61, 1);
  -moz-box-shadow: 8px 8px 24px 0px rgba(20, 33, 61, 1);
  box-shadow: 8px 8px 24px 0px rgba(20, 33, 61, 1);
`;

export default StyledQuoteCard;
